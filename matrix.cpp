#include <iostream>
#include <stdlib.h>

using namespace std;

int main() {
    int Rows, Columns;
    cout<<"Enter the rows of the numbers matrix: "; cin>>Rows;
    cout<<"Enter the columns of the matrix: "; cin>>Columns;
    int matrix[Rows][Columns];

    for(int i = 0; i < rows; i++) {
        for(int j = 0; i < columns; j++) {
            cout<<"Row "<<i<<", Column "<<j<<": "
        }
    }


    return 0;
}